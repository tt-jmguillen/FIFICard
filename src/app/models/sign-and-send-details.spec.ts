import { SignAndSendDetails } from './sign-and-send-details';

describe('SignAndSendDetails', () => {
  it('should create an instance', () => {
    expect(new SignAndSendDetails()).toBeTruthy();
  });
});
